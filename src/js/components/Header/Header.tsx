import * as React from 'react'
import { HeaderMenu } from '../HeaderMenu/HeaderMenu';




export const Header = () => {
    return (
        <header className="header">
            <HeaderMenu />
        </header>
    )
}